import { Box } from "@mui/material";
import React from "react";

const SubHeader = () => {
  return (
    <Box height={40} bgcolor="white" p={1}>
      SUB_HEADER_SECTION
    </Box>
  );
};

export default SubHeader;
